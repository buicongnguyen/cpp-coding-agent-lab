--- a/src/types.cpp
+++ b/src/types.cpp
@@ -15,18 +15,97 @@
 } // namespace
 
 Json message_to_json(const Message& message) {
+    Json result = Json::object();
+    result["role"] = message.role;
+
+    if (!message.content.empty()) {
+        result["content"] = message.content;
+    } else if (message.role == "assistant") {
+        result["content"] = Json(nullptr);
+    }
+
+    if (!message.tool_calls.empty()) {
+        Json calls = Json::array();
+        for (const ToolCall& call : message.tool_calls) {
+            Json function = Json::object();
+            function["name"] = call.name;
+            function["arguments"] = call.arguments.dump();
+
+            Json encoded = Json::object();
+            encoded["id"] = call.id;
+            encoded["type"] = "function";
+            encoded["function"] = std::move(function);
+            calls.push_back(std::move(encoded));
+        }
+        result["tool_calls"] = std::move(calls);
+    }
+
+    if (!message.tool_call_id.empty()) result["tool_call_id"] = message.tool_call_id;
+    if (!message.name.empty()) result["name"] = message.name;
+    return result;
-    (void)message;
-    throw std::runtime_error("TODO checkpoint 01: serialize message history");
 }
 
 Message assistant_message_from_json(const Json& value) {
+    if (!value.is_object()) throw std::runtime_error("Assistant message must be a JSON object");
+
+    Message result;
+    result.role = value.contains("role") ? value.at("role").string_or("assistant") : "assistant";
+    if (result.role != "assistant") throw std::runtime_error("Model response message role must be assistant");
+    if (value.contains("content") && value.at("content").is_string()) result.content = value.at("content").as_string();
+
+    if (value.contains("tool_calls")) {
+        if (!value.at("tool_calls").is_array()) throw std::runtime_error("tool_calls must be an array");
+        std::set<std::string> call_ids;
+        for (const Json& encoded : value.at("tool_calls").as_array()) {
+            if (!encoded.is_object()) throw std::runtime_error("Each tool call must be a JSON object");
+            ToolCall call;
+            call.id = encoded.at("id").as_string();
+            if (call.id.empty() || !call_ids.insert(call.id).second) {
+                throw std::runtime_error("Tool call IDs must be non-empty and unique within a response");
+            }
+            const Json& function = encoded.at("function");
+            if (!function.is_object()) throw std::runtime_error("Tool call function must be a JSON object");
+            call.name = function.at("name").as_string();
+            if (call.name.empty()) throw std::runtime_error("Tool call function name must not be empty");
+            const Json& arguments = function.at("arguments");
+            call.arguments = arguments.is_string() ? Json::parse(arguments.as_string()) : arguments;
+            if (!call.arguments.is_object()) throw std::runtime_error("Tool call arguments must decode to a JSON object");
+            result.tool_calls.push_back(std::move(call));
+        }
+    }
+
+    return result;
-    (void)value;
-    throw std::runtime_error("TODO checkpoint 01: parse assistant messages");
 }
 
 ModelResponse model_response_from_json(const Json& value) {
+    if (!value.is_object()) throw std::runtime_error("Model response must be a JSON object");
+    if (!value.contains("choices") || !value.at("choices").is_array() || value.at("choices").as_array().empty()) {
+        throw std::runtime_error("Model response contains no choices");
+    }
+
+    const Json& choice = value.at("choices").at(0);
+    ModelResponse result;
+    result.assistant = assistant_message_from_json(choice.at("message"));
+    result.finish_reason = choice.contains("finish_reason") ? choice.at("finish_reason").string_or("") : "";
+    result.model = value.contains("model") ? value.at("model").string_or("") : "";
+
+    if (value.contains("usage") && value.at("usage").is_object()) {
+        const Json& usage = value.at("usage");
+        if (usage.contains("prompt_tokens")) result.usage.prompt_tokens = size_from_number(usage.at("prompt_tokens"));
+        if (usage.contains("completion_tokens")) result.usage.completion_tokens = size_from_number(usage.at("completion_tokens"));
+        if (usage.contains("cost")) result.usage.cost = usage.at("cost").number_or(0.0);
+
+        if (usage.contains("completion_tokens_details") && usage.at("completion_tokens_details").is_object()) {
+            const Json& details = usage.at("completion_tokens_details");
+            if (details.contains("reasoning_tokens")) result.usage.reasoning_tokens = size_from_number(details.at("reasoning_tokens"));
+        }
+        if (usage.contains("prompt_tokens_details") && usage.at("prompt_tokens_details").is_object()) {
+            const Json& details = usage.at("prompt_tokens_details");
+            if (details.contains("cached_tokens")) result.usage.cached_tokens = size_from_number(details.at("cached_tokens"));
+        }
+    }
+
+    return result;
-    (void)value;
-    throw std::runtime_error("TODO checkpoint 01: parse model responses");
 }
 
 } // namespace course_agent
