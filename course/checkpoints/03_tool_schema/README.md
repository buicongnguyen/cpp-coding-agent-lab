# Checkpoint 03 — Tool schemas

## Add

- `ToolDispatcher::definitions()` only; execution may still be stubbed.
- `parallel_tool_calls: false` in live request construction.

## Learner task

Complete the schemas for `read_file`, `write_file`, and symbolic `run_command(action)`.

## Release gate

Schemas have required fields, reject additional properties, and state their workspace boundaries.

